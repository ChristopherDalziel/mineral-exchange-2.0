<div class="container">
  <%= render '/pages/nav' %>

   <% if user_signed_in? %>
       <%= link_to 'Create New Listing', new_listing_path %>
          <% else %>
            <%= link_to 'Login To Make A New Listing', new_user_session_path %>
    <% end %>





   <!-- (:user_id, :type_id :mineral_name, :description, :location, :price, :image) -->

  <table>
      <thead>
        <tr>
          <th>Id:</th>
          <th>User Id:</th>
          <th>Type:</th>
          <th>Mineral:</th>
          <th>Description:</th>
          <th>Location:</th>
          <th>Price:</th>
          <th>Image:</th>
          <th colspan="3"></th>
        </tr>
      </thead>
      <tbody>
        <% @listings.each do |listing| %>
          <tr>
            <td><%= listing.id %></td>
            <td><%= listing.user.email%>
            <td><%= listing.type.name %></td>
            <td><%= listing.mineral_name %></td>
            <td><%= listing.description %></td>
            <td><%= listing.location %></td>
            <td><%= listing.price %></td>
            <td><%= listing.image %></td>
            <% if current_user == listing.user %>
              <td><%= link_to 'Edit', edit_listing_path(listing) %></td>
              <td><%= link_to 'Show', listing_path(listing) %></td>
              <td><%= link_to 'Destroy', listing_path(listing), method: :delete, data: { confirm: 'Are you sure?' } %></td>
            <% end %>
              
          </tr
        <% end %>
      </tbody>
    </table>

  <%= render '/pages/footer' %>

</div>